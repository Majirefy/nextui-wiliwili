# trimui-portlib

Smart Pro TG5040 portlib

```shell
sudo apt-get install binfmt-support qemu-user-static cmake meson wget

sudo tar zxf aarch64-linux-gnu-7.5.0-linaro.tgz -C /opt/trimui --strip-components=1
sudo tar zxf SDK_usr_tg5040_a133p.tgz -C /opt/trimui
```

> [meson](https://mesonbuild.com/Machine-files.html)
